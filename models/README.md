# Local Models

This repository never stores model weights.

Downloaded models belong under `models/local/`, which is ignored by Git, or in an external user-controlled directory. Registry entries record model identity, capability, provenance, license review, and benchmark status without embedding private machine paths.

Only models that pass the feasibility milestone should be enabled by product configuration.

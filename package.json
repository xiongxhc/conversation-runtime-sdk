{
  "name": "conversation-runtime-sdk",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "desktop:dev": "npm run tauri:dev --workspace conversation-desktop --",
    "test": "npm test --workspaces"
  },
  "devDependencies": {
    "@types/node": "18.19.39",
    "typescript": "5.5.4"
  }
}
